scalar DateTime

type File {
  id: ID!
  filename: String!
  mimeType: String!
  size: Int!
  path: String!
  expiresAt: DateTime!
  createdAt: DateTime!
}

type Query {
  files: [File!]!
  file(id: ID!): File
}
